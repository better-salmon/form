{
  "$schema": "./node_modules/turbo/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "dist-ssr/**", "build/**"]
    },
    "lint": {
      "dependsOn": ["form#build", "^lint"]
    },
    "check-types": {
      "dependsOn": ["form#build", "^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
